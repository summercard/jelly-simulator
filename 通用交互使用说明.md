# v009 双盘移动与双向挤压

打开 `Q弹果冻_双盘双向挤压_v009.blend`，插件版本4.2.0。

## 直接操作

1. 选择薄荷色盘子，按G或拖动移动箭头；盘子会带着整只果冻移动。也可在面板选择当前编号后点“移动当前盘子和果冻”。
2. 左右各有一个蓝色BOX，可独立沿X轴向中间推。
3. 面板顶部“两侧方块向中间挤压”滑块控制两个BOX同步推进，0为撤回，0.65为已验证的相互挤压测试位置。
4. 选择软糯或偏硬预设，比较压痕传播和撤回后的恢复；阻尼控制余晃，鼓出程度与扩散范围继续参与计算。测试场景默认关闭重力以方便观察，可重新开启。

## 碰撞与切面

盘子没有碰撞约束，盘沿可以重叠。果冻之间使用双向接触约束，并在显示网格输出后再次处理接触。标准并排、切面同向的两只果冻使用共同接触面稳定分离；其他姿态及自定义软物体使用网格接触。

切面维持固定。两个固定切面不能被强行拖进同一位置，因此同平面并排拖动时会限制果冻切面的重合；该限制来自果冻边界，不是盘沿碰撞。此规则按原始单位尺寸果冻设置，不适用于随意缩放后的切面。

方块是位置驱动：同样的方块位置会强制满足接触，硬度主要改变形变传播和恢复，而不是把方块反向弹开。深度参数继续控制球体/替换工具；双BOX有独立的推进滑块。

## 验证

`v009_两侧挤压验证.json`记录两只果冻的实际软体接触计数和显示网格穿插检测。0.65推进测试中，两只果冻均产生相互接触，没有检出超过0.003场景单位容差的顶点穿插。

`v009_盘子移动与手感验证.json`记录盘子带动整只果冻的位移误差、切面重合限制和硬度差异。测试覆盖渐进移动；本原型仍不保证任意快速穿越、极端夹压或复杂凹面下完全无穿透。

以下为v008通用功能说明，仍可使用。

# v008 通用软体表面交互

打开 `Q弹果冻_通用软体交互_v008.blend`。插件4.1.0由 `kawaii_jelly_interaction.py` 和 `jelly_surface_solver.py` 两个文件组成；请完整安装同目录ZIP，然后重启Blender。

## 本版修复

- 不增加显示网格面数：仍为4610顶点。计算网格位移采用12邻点连续权重插值，显示网格交替执行位移平滑和接触约束，减少单点投影导致的褶皱。
- 切面同时在计算网格和最终显示网格锁定，切面附近有平滑过渡。球体靠近切面时优先限制交互物体位置，不能靠移动切面让出空间。
- 调整“挤压深度”会自动进入按压模式；“可压入程度”在通用模式中重新生效，控制原球体及一键替换工具的最大压入限制。
- 球体、方块、椭球、骨骼手支持一键替换，原工具隐藏并停止碰撞，重复选择会复用。预置骨骼手已包含在主场景中。

## 保留的手感

原有果冻抓拉、快甩、硬度、阻尼、重力下垂、固定切面和球体三轴控制保留。新版场景默认启用“通用软体表面求解”。关闭它可回到v006球体专用贴合算法，便于对照。

通用模式使用网格边长、弯曲平滑、体积和接触约束产生挤压。它保留原手感参数，但不同求解器的具体压痕、瞬态和回弹不会与v006逐点一致。

## 不规则交互物体

1. 添加或导入封闭网格，确保法线向外。
2. 选中它，在N侧栏“Q弹果冻 → 通用表面交互”点击“选中网格设为交互物体”。
3. 使用Blender标准G、R、S或三轴箭头移动、旋转、缩放。果冻按实际变形后的表面响应。
4. 移开物体，果冻弹性恢复；“移除选中物体的交互”可取消标记。

面板提供“球体”“方块”“椭球”“骨骼手”一键替换按钮。替换后共用原三轴控制柄、自动按压和深度参数；也可直接移动替换物体。自行导入并标记的外部网格仍由自身变换控制，不受这个工具控制柄的深度限制。

“挤压深度”控制当前替换工具的自动推进；手动拖动时调整这个参数会切回自动按压。“可压入程度”控制最大压入限制，达到硬度或切面安全上限后，继续增大目标深度不会继续深入。通用模式的硬度控制恢复与形状约束；运动学交互物体的位置由用户/动画决定，不会被反作用力自动推回。

## 两个软物体相互挤压

选择另一个独立的封闭网格，点击“选中网格设为软物体”。它会获得独立的模拟状态；与其他软物体接触时，双方分担接触位移并变形。

- 使用独立网格；转换前先应用修改器并解除父级。
- 最多6个软物体。高密度模型使用约900顶点的计算网格，将位移插值回原网格。
- 新软物体以导入形状和基座为恢复目标。移动其自动建立的“软物体基座”来改变目标位置。
- 原果冻切面继续固定。新增软物体没有固定切面，但有回到基座形状的弹性约束；不是自由落体布娃娃。
- 骨骼模型请使用“交互物体”路径；当前不支持同一网格同时由骨骼和本求解器进行双向软体驱动。

## 骨骼、形态键与手

交互物体每次更新都读取Blender求值后的网格，因此Armature、Shape Keys和修改器的表面变化会参与碰撞。动画播放或手动Pose变形都可以。

`Q弹果冻_骨骼手掌演示_v008.blend` 提供简化手掌和五根手指。面板“示例手指弯曲”滑块驱动骨骼；也可选骨骼进入Pose模式。示例用于验证多接触面，不是解剖学完整的手模型。

骨骼手是运动学驱动：手的变形影响果冻，果冻不会自动改变手的骨骼姿态。若需要肌肉、皮肤与果冻双向受力，需要进一步加入骨骼/皮肤动力学耦合。

## 参数与性能

- 硬度、阻尼：保持原有硬软和恢复控制。
- 鼓出程度：控制接触损失体积的补偿目标；1为体积保持目标。
- 鼓出扩散范围：控制补偿集中在接触附近的范围。
- 接触迭代次数：越高约束更充分，耗时也更高；默认3。

本版是表面约束软体原型，不是完整实体有限元模拟。极薄、开放、自相交网格、尖锐凹角、深度夹压和突然跨越整个物体的运动可能不稳定。移动会做有限的插值子步，不能保证任意速度下无穿透。体积保持是迭代目标，不是每帧严格恒定。

实时速度取决于顶点数、接触面积和交互物体数量。原果冻模型的测量记录见 `通用软体_实际场景验证.json`，这是后台求解耗时，不包含完整UI绘制成本。

## 验证与文件

- `通用软体_网格骨骼双向交互测试.json`：方块接触、形态键更新、骨骼更新、恢复、不规则软物体双向形变、面板绘制。
- `通用软体_实际场景验证.json`：原果冻固定切面、计算/显示网格规模与耗时。
- `previews_v008/`：原果冻上的圆角方块、骨骼手和切面附近侧压图。
- `v008_切面深度替换验证.json`：三个方向的最终切面位置、深度参数和工具复用。
- `v008_同面数侧压平滑对照.json`：相同侧压条件下v007与v008的位移拉普拉斯RMS对比。此案例降低约55.5%，不代表任意姿态都完全无褶皱。

重新打开时模拟状态重建；互动仍未烘焙为动画。可在Blender后台运行 `scripts/test_generic_surface.py` 复测。


## v010 控件修复与性能优化

打开 `Q弹果冻_控件修复与优化_v010.blend`；安装更新后请重启 Blender，N 侧栏选择 Q弹果冻。修正 Blender 4.2 不支持的盘子按钮图标，恢复后续全部控件。

缓存约束长度、邻接度数和未变化碰撞体的 BVH；接触体积计算不再重复计算用不到的梯度。骨骼与形态键仍逐步读取实际评估网格。网格面数、子步规则、迭代次数和平滑参数均未降低。65 步双向挤压的优化前后显示顶点差为 0，首次实测平均求解耗时 92.7 → 75.6 ms（约减少 18%，不含完整 UI 绘制）。复杂接触仍可能卡顿。复测脚本：`scripts/test_v010_performance.py`。
