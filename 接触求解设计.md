# v007 已接入通用表面约束求解

新模块 `jelly_surface_solver.py` 实现边长约束、位移平滑、体积恢复、求值网格接触和双向软体接触。使用实际骨骼/形态键变形后的表面，已不依赖球面角度映射。大网格通过计算网格驱动原模型。该实现是带柔顺性的表面位置约束原型，不是完整XPBD实体体积网格或FEM。

旧版球体算法保留为兼容模式。以下v006设计记录中的“未来接入”部分属于历史计划，当前能力与限制见《通用交互使用说明.md》。

# v006 表面贴合与后续形状扩展

## 当前实现

球体压入位置由接触检测和硬度限幅确定。接触网格使用沿球面的连续参数映射，外侧通过切线连续的过渡连接未受压表面；体积补偿只改变自由表面，不把已贴合的接触区重新切回球体后半球。贴合区立即满足物体表面约束，其余组织保留弹簧恢复。

接触中的球体不再被第二次移出自己已经求解好的凹坑；否则球体位置与果冻接触面不一致，会留下空槽。球体与网格之间保留0.0025场景单位的数值间距。场景中的本体细分一次，球体提高到96段，以降低多边形离散造成的穿插和锯齿。

这是交互形变近似，不是完整连续体力学。固定切面、极端压入和多物体同时夹压需要更强的约束求解；本版不支持多个挤压器。

## 统一接口

`query(points) -> nearest, normals, signed_distance`

- 查询点和碰撞表面必须使用同一坐标空间。
- 有符号距离在物体外为正，在内部为负。
- `project_contact_surface` 负责通用的单边防穿透投影；物体外侧的自由点不吸附。
- `SphereContactSurface` 提供当前球体解析查询。
- `MeshContactSurface` 提供封闭、外向法线网格的 BVH 查询；已经用立方体验证内部、表面及外部点。

## 未来接入其他形状

球体的接触面参数映射仍是球形专用。仅替换表面适配器，并不足以宣称任意形状挤压已完成。

胶囊、圆角方块或自定义网格需要额外实现：

1. 基于实际几何体的连续碰撞检测与接触片区生成，替换当前球形半径扫掠。
2. 在接触片区上按最近表面和切向滑动构造无翻折映射，替换球面角度映射。
3. 按接触边界分布体积补偿，保持接触区贴面、自由区弹性恢复。
4. 对尖角、凹面、缩放和多个接触片区进行穿透、法线方向与网格翻折测试。

应复用表面约束、体积测量、弹簧状态和恢复过程；不得给每种形状另画一个固定圆环鼓包。
